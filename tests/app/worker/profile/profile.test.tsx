
import React from 'react';
import { render, screen } from '@testing-library/react';
import WorkerProfilePage from '@/app/worker/profile/[id]/page';

describe('WorkerProfilePage', () => {
    it('is a placeholder test', () => {
        expect(true).toBe(true);
    });
});
